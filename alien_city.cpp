#include "alien_city.h"

AlienCity::AlienCity(std::string name) {
    // Initialize the objects map
    objects = {
        {1, "computer"},
        {2, "weapon"},
        {3, "artifact"},
        {4, "transportation device"}
    };

    // Initialize the name member variable
    // this->name = name;
}
